% Reference Manual §9.5 — SELECT statement

SECTION ref_select;

TYPE integer {-32768 TO 32767};

DCL pick PROC(n integer) RETURNS integer IS
   BLOCK
      SELECT n IN
         {1}: RETURN 10;
         {2}: RETURN 20;
         OUT RETURN 0;
      ENDSELECT;
   ENDBLOCK;